# Data

The project loads scikit-learn's bundled Iris classification dataset at runtime. It makes no
network request and stores no personal data. Run `python scripts/export_dataset.py` to export a
CSV copy for inspection or upload testing.

Created by School of AI and School of QC.
